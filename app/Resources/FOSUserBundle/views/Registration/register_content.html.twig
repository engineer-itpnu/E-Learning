{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form) }}
{{ form_widget(form) }}
<br/>
<div style="width: 100%;text-align: center"><input type="submit" class="btn btn-large btn-primary" value="ثبت نام"></div>
{{ form_rest(form) }}
{{ form_end(form) }}