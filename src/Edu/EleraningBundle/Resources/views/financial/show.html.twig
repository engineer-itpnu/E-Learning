{% extends '::base.html.twig' %}

{% block body -%}
    <h1>financial</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Money</th>
                <td>{{ entity.money }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Adddate</th>
                <td>{{ entity.adddate|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Explanation</th>
                <td>{{ entity.explanation }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('financial') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('financial_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
