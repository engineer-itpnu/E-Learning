{% extends '::base.html.twig' %}

{% block body -%}
    <h1>financial list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Money</th>
                <th>Date</th>
                <th>Adddate</th>
                <th>Explanation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('financial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.money }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.adddate %}{{ entity.adddate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.explanation }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('financial_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('financial_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('financial_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
