{% extends '::base.html.twig' %}

{% block body -%}
    <h1>lesson list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Explaination</th>
                <th>Password</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lesson_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.explaination }}</td>
                <td>{{ entity.password }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('lesson_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('lesson_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('lesson_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
